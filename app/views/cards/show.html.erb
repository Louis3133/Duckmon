<p style="color: green"><%= notice %></p>


<div class="container">
  <%= render @card %>
</div>

<div class="container">
  <%= link_to "Modifier la carte", edit_card_path(@card), class:"btn btn-warning" %>
  <%= link_to "retour aux cartes", cards_path, class:"btn btn-primary" %>

  <%= button_to "supprimer la carte", @card, method: :delete, class:"btn btn-danger" %>
</div>
